# Telegram Бот для Бронирования

📚 Современный Telegram-бот для управления записями на услуги/консультации с интерактивным календарем, автоматическими напоминаниями и аналитикой.

## ✨ Основные возможности

### Для пользователей:
- 📅 **Интерактивный календарь** с индикатором загрузки слотов
- 🔄 **Перенос записей** в 2 клика
- ⏰ **Автоматические напоминания** за 24 часа до встречи
- ⭐ **Система обратной связи** после встречи
- 👥 **Личный кабинет** с историей записей
- ⚠️ **Защита от овербукинга** (лимит 3 записи одновременно)

### Для администраторов:
- 📊 **Dashboard** с ключевыми метриками
- 📅 **Расписание** на 7 дней вперед
- 👥 **Аналитика клиентов** (ТОП-10 по записям)
- 📢 **Массовая рассылка** сообщений
- 📊 **Экспорт данных** в CSV
- 💡 **AI-рекомендации** на основе аналитики

## 🛠️ Технологии

- **Python 3.10+**
- **aiogram 3.4.1** - современный фреймворк для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **APScheduler** - планировщик задач для напоминаний
- **python-dotenv** - управление переменными окружения

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/balzampsilo-sys/tg-bot.git
cd tg-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корневой директории:

```env
# Токен бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram ID администратора (получить можно у @userinfobot)
ADMIN_ID=123456789
```

#### Как получить токен бота:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (введите имя и username бота)
4. Скопируйте полученный токен

#### Как узнать свой Telegram ID:

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите "Start"
3. Бот покажет ваш ID

### 5. Запуск бота

```bash
python main.py
```

При успешном запуске вы увидите:
```
🚀 Bot started
```

## 📚 Структура проекта

```
tg-bot/
│
├── handlers/              # Обработчики событий
│   ├── user_handlers.py   # Команды пользователя (/start, /help)
│   ├── booking_handlers.py # Бронирование и перенос
│   └── admin_handlers.py  # Админ-панель
│
├── keyboards/            # Клавиатуры
│   ├── user_keyboards.py  # Клавиатуры пользователя
│   └── admin_keyboards.py # Клавиатуры админа
│
├── database/             # Работа с БД
│   ├── models.py          # Модели данных
│   └── queries.py         # SQL-запросы
│
├── services/             # Бизнес-логика
│   ├── booking_service.py # Управление записями
│   ├── notification_service.py # Уведомления
│   └── analytics_service.py # Аналитика
│
├── utils/                # Вспомогательные функции
│   ├── helpers.py         # Вспомогательные функции
│   └── states.py          # FSM состояния
│
├── config.py             # Конфигурация
├── main.py               # Точка входа
├── requirements.txt      # Зависимости
├── .env                  # Переменные окружения (не в Git)
└── .gitignore            # Игнорируемые файлы
```

## ⚙️ Настройка

Все настройки находятся в файле `config.py`:

```python
# Настройки бронирования
MAX_BOOKINGS_PER_USER = 3        # Макс одновременных записей
CANCELLATION_HOURS = 24          # Минимальное время до отмены
WORK_HOURS_START = 9             # Начало рабочего дня
WORK_HOURS_END = 19              # Конец рабочего дня

# Настройки услуги
SERVICE_DURATION = "1 час"
SERVICE_LOCATION = "г. Москва, ул. Примерная, 1 / Онлайн"
SERVICE_PRICE = "3000 ₽"
```

## 💻 Использование

### Команды пользователя:

- `/start` - Запуск бота и онбординг
- `📅 Записаться` - Создать новую запись
- `📋 Мои записи` - Посмотреть активные записи
- `ℹ️ О сервисе` - Информация об услуге

### Команды администратора:

- `/admin` - Вход в админ-панель
- `📊 Dashboard` - Статистика бота
- `📅 Расписание` - Расписание на неделю
- `👥 Клиенты` - Статистика клиентов
- `⚡ Массовые операции` - Рассылка, очистка
- `📊 Экспорт данных` - Выгрузка в CSV
- `💡 Рекомендации` - AI-рекомендации

## 💾 База данных

Бот использует SQLite (файл `bookings.db`) со следующими таблицами:

- `bookings` - Записи пользователей
- `users` - Регистрация пользователей
- `analytics` - События и действия пользователей
- `feedback` - Отзывы от клиентов
- `blocked_slots` - Заблокированные слоты
- `admin_sessions` - Сессии администраторов

База данных автоматически создается при первом запуске бота.

## 🔧 Разработка

### Запуск в режиме разработки:

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Запустите бота
python main.py
```

### Проверка синтаксиса:

```bash
python -m py_compile handlers/*.py
python -m py_compile services/*.py
python -m py_compile database/*.py
```

## 🐛 Известные проблемы и ограничения

- SQLite не подходит для высоконагруженных проектов (рекомендуется PostgreSQL)
- Отсутствует система миграций БД (Alembic)
- Нет автоматических тестов (pytest)
- Нет rate limiting (защиты от спама)

## 🛡️ Безопасность

- Не коммитьте файл `.env` в Git
- Используются параметризованные SQL-запросы (защита от SQL-инъекций)
- Проверка прав доступа для админ-команд
- Валидация всех callback_data

## 📄 Лицензия

MIT License - свободно используйте в своих проектах.

## 👥 Автор

Разработано [balzampsilo-sys](https://github.com/balzampsilo-sys)

## 💬 Поддержка

Если у вас есть вопросы или предложения:

1. Откройте [Issue](https://github.com/balzampsilo-sys/tg-bot/issues) на GitHub
2. Создайте [Pull Request](https://github.com/balzampsilo-sys/tg-bot/pulls) с улучшениями

---

**Сделано с ❤️ для Telegram**
